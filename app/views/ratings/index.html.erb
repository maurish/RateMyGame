<h1>Listing ratings</h1>

<table>
  <thead>
    <tr>
      <th>Game</th>
      <th>Score</th>
      <th></th>
      <th></th>
      <th></th>
    </tr>
  </thead>

  <tbody>
    <% @ratings.each do |rating| %>
      <tr>
        <td><%= rating.game.name %></td>
        <td><%= rating.score %></td>
        <td><%= button 'Show', rating %></td>
        <td><%= button 'Edit', edit_rating_path(rating) %></td>
        <td><%= link_to 'Destroy', rating, method: :delete, data: { confirm: 'Are you sure?' } %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>

<%= button 'New Rating', new_rating_path, "btn btn-primary" %>
